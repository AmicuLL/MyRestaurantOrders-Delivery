---Initialize the server: npm init -y
---Install required modules: npm install express node-json-db

---init.bat will initialize the project and will install the necessary modules.

---startServer.bat will start the server and the web page can be accesed at this link: localhost:8080 (http://localhost:8080/)